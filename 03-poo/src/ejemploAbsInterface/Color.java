package ejemploAbsInterface;

/*
    Enum -> Tipos especiales para definir un grupo fijo de valores constantes con nombre, como días de la semana o colores,
    que no cambian y facilitan el manejo claro y seguro de esos valores en el código.
 */
public enum Color {
    RED,
    GREEN,
    BLUE,
    WHITE,
    BLACK
}
